<h2><a href=https://www.codewars.com/kata/56426b4e971f01fd2f00005b/train/javascript target="_blank">Find the Square Root</a></h2><h3>7 kyu</h3><p>Create a Function that takes one parameter and returns its square root rounded to 5 decimal places. You are not allowed to use the Math.sqrt() or the Math.pow() methods.</p><p>Trailing zeros should be left out. For example</p><pre><code class="language-javascript"><span class="cm-variable">squareRoot</span>(<span class="cm-number">39</span>) <span class="cm-comment">// =&gt; 6.245;</span></code></pre><p>rather than</p><pre><code class="language-javascript"><span class="cm-variable">squareRoot</span>(<span class="cm-number">39</span>) <span class="cm-comment">// =&gt; 6.24500;</span></code></pre>